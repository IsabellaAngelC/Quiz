const studentGrades = [100, 50, 65, 70, 85, 96, 45];
const result = computeGrades(studentGrades);
console.log(result);

studentGrades.forEach((grade) => {
	if (grade > 90 && grade < 100) {
		return 'A';
		consolele.log(computeGrades);
	}
});

const A = passed;
const B = passed;
const C = passed;
const D = passed;
const F = lose;
